# TaskTrack

A program that displays a menu to view and add tasks.

## Current Features

-Add and view tasks.
-[Feature]

## Running the Program

run tasktrack.py
enter desired menu number to either view, or add tasks to the task list.

## Current Limitation

All tasks are only stored in active memory, so when the program closes, all tasks are NOT saved.